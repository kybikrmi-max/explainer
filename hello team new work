/* 
  ============================================================
  MODERN BENTO GRID PORTFOLIO THEME
  ============================================================
  
  A responsive, dark-themed portfolio layout using CSS Grid
  with a Bento Box design pattern. Features smooth animations,
  flexible spacing, and accessible typography.
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Portfolio | Bento Grid Template</title>
    <link rel="stylesheet" href="style.css">
</head>
<body>
    <div class="bg-glow"></div>
    <main class="bento-container">
        
        <!-- HERO CARD -->
        <div class="card hero reveal">
            <div>
                <div class="eyebrow">Welcome to Portfolio</div>
                <h1>Your Name<br>Here.</h1>
                <p style="max-width: 300px;">Describe your expertise and unique value proposition. Highlight what makes you different and <span class="accent-color">what you specialize in</span>.</p>
            </div>
            <div style="margin-top: 20px;">
                <a href="#projects" class="btn" style="background: var(--accent-color); border-color: var(--accent-color); color: #000; margin-right: 10px;">View Work</a>
                <a href="#contact" class="btn" style="border-color: #555; color: #fff;">Get in Touch</a>
            </div>
        </div>

        <!-- ABOUT CARD -->
        <div id="about" class="card about reveal">
            <div class="eyebrow">Philosophy</div>
            <div>
                <h2>Your Approach</h2>
                <p>Explain your core approach and what guides your work.</p>
            </div>
            <div class="tag-cloud">
                <span class="tag">Strategic</span>
                <span class="tag">Creative</span>
                <span class="tag">Results-Driven</span>
            </div>
        </div>

        <!-- SKILLS CARD -->
        <div id="skills" class="card skills reveal">
            <div class="eyebrow">Expertise</div>
            <div class="tag-cloud">
                <span class="tag">Skill One</span>
                <span class="tag">Skill Two</span>
                <span class="tag">Skill Three</span>
            </div>
        </div>

        <!-- IMPACT CARD 1 -->
        <div id="impact" class="card impact reveal">
            <div class="eyebrow">Achievement</div>
            <div>
                <div class="stat-number">100+</div>
                <p>Projects Completed</p>
            </div>
        </div>

        <!-- PROJECTS CARD -->
        <div id="projects" class="card projects reveal">
            <div class="eyebrow">Selected Works</div>
            <div style="width: 100%;">
                <div class="project-item">
                    <span>Project Name One</span>
                    <span class="tag">Category

/**
 * PORTFOLIO INTERACTIVITY - Scroll Reveal & Effects
 */

document.addEventListener('DOMContentLoaded', () => {
    
    // ========================================
    // 1. SCROLL REVEAL ANIMATION
    // ========================================
    // Adds 'active' class when elements enter viewport
    const revealElements = document.querySelectorAll('.reveal');
    
    const revealOnScroll = () => {
        const windowHeight = window.innerHeight;
        revealElements.forEach(el => {
            const elTop = el.getBoundingClientRect().top;
            const revealPoint = 150; // Trigger 150px before element reaches view
            if (elTop < windowHeight - revealPoint) {
                el.classList.add('active');
            }
        });
    };
    
    window.addEventListener('scroll', revealOnScroll);
    revealOnScroll(); // Trigger on load

    // ========================================
    // 2. SMOOTH SCROLL FOR ANCHOR LINKS
    // ========================================
    document.querySelectorAll('a[href^="#"]').forEach(anchor => {
        anchor.addEventListener('click', function (e) {
            e.preventDefault();
            const target = document.querySelector(this.getAttribute('href'));
            if (target) {
                window.scrollTo({
                    top: target.offsetTop - 80,
                    behavior: 'smooth'
                });
            }
        });
    });

    // ========================================
    // 3. DYNAMIC NAV BACKGROUND (Optional)
    // ========================================
    const nav = document.querySelector('nav');
    if (nav) {
        window.addEventListener('scroll', () => {
            if (window.scrollY > 50) {
                nav.style.background = 'rgba(5, 5, 5, 0.95)';
                nav.style.padding = '1rem 0';
            } else {
                nav.style.background = 'rgba(5, 5, 5, 0.8)';
                nav.style.padding = '1.5rem 0';
            }
        });
    }

    // ========================================
    // 4. MOUSE-TRACKING GLOW EFFECT (Optional)
    // ========================================
    const body = document.body;
    body.addEventListener('mousemove', (e) => {
        const x = e.clientX / window.innerWidth;
        const y = e.clientY / window.innerHeight;
        
        const glow = document.querySelector('.bg-glow');
        if (glow) {
            glow.style.background = `radial-gradient(circle at ${x * 100}% ${y * 30}%, var(--accent-color) 0%, transparent 70%)`;
        }
    });

});
✅ Fully Generic - No specific names or branding
✅ Detailed Comments - Explains every section
✅ Easy to Customize - Clear placeholders marked
✅ Responsive - Mobile, tablet, desktop support
✅ Modern Features - Smooth animations, interactions
✅ Production Ready - No dependencies needed
